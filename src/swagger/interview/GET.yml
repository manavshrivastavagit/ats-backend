Return an interview key information based on his name
---
tags:
  - interview
parameters:
  - name: last_name
    in: path
    type: string
    description: the last name of the interview
  - name: first_name
    in: path
    type: string
    description: the last name of the interview
  - name: page_number
    in: query
    type: string
    description: page_number
  - name: per_page_data
    in: query
    type: string
    description: per_page_data
  
responses:
  200:
    description: The interview's information were successfully retrieved
    schema:
      example:
        interview:
          last_name: Doe
          first_name: John
          # age: 30
