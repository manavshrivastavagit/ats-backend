Create an hr based on the sent information
---
tags:
  - hr
parameters:
  - name: last_name
    in: path
    type: string
    description: the last name of the hr
  - name: first_name
    in: path
    type: string
    description: the last name of the hr
  - name: body
    in: body
    schema:
      type: object
      properties:
        # age:
        #   type: integer
        #   description: The age of the hr
responses:
  200:
    description: The hr was successfully created
    schema:
      example:
        hr:
          last_name: Doe
          first_name: John
          # age: 30
